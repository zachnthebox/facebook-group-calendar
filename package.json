{
  "name": "facebook-group-calendar",
  "version": "1.0.0",
  "description": "I've had a dream.",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "main": "node dist/main.js",
    "premain": "babel src --out-dir dist",
    "postinstall": "mkdir -p dist && babel src --out-dir dist"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "fb": "^1.0.2",
    "googleapis": "5.1.0",
    "moment": "^2.12.0",
    "moment-timezone": "^0.5.3",
    "request": "^2.72.0",
    "rsvp": "^3.2.1"
  }
}
